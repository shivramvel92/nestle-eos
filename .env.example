# Anthropic API Key (required for AI features)
ANTHROPIC_API_KEY=sk-ant-YOUR_KEY_HERE

# Port (Railway sets this automatically)
PORT=3001

# Optional: Override API URL in React (leave blank for same-origin)
REACT_APP_API_URL=
