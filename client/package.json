{
  "name": "nestle-eos-server",
  "version": "1.0.0",
  "description": "Nestlé HR·EOS — Enterprise AI Operating System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:client": "cd client && npm install && npm run build",
    "postinstall": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
    {
  "proxy": "http://localhost:3001"
  }
}
