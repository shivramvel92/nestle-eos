[phases.setup]
nixPkgs = ["nodejs_22", "python3", "pkg-config", "gcc", "gnumake"]

[phases.install]
cmds = [
  "npm install",
  "cd client && npm install"
]

[phases.build]
cmds = [
  "cd client && npm run build"
]

[start]
cmd = "node server.js"